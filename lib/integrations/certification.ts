import type { IntegrationCertification, IntegrationCertificationCheck, IntegrationDefinition, IntegrationSupportLevel } from "@/lib/integrations/types";
const checks:IntegrationCertificationCheck[]=["credential","connection","failure","recovery","security","functional"];
export function certifyIntegration(definition:IntegrationDefinition,certification:IntegrationCertification):IntegrationDefinition{const missing=checks.filter(check=>!certification.checks[check]?.passedAt||!certification.checks[check]?.suite);if(missing.length)throw new Error(`Integration cannot be certified; missing passing evidence: ${missing.join(", ")}.`);if(!certification.certifiedAt)throw new Error("Integration cannot be certified without a certification timestamp.");return{...definition,certification};}
export function integrationLabel(definition:IntegrationDefinition){if(definition.certification?.certifiedAt&&checks.every(check=>definition.certification?.checks[check]))return"Certified";if(definition.maturity==="adapter")return"Adapter available — not certified";if(definition.category==="unknown")return"Unknown integration";return"Metadata only";}
export function integrationSupportLevel(definition:IntegrationDefinition,evidence:{detected?:boolean;guided?:boolean;credentialsValidated?:boolean;runtimeExecuted?:boolean;sandboxE2E?:boolean;hardwareValidated?:boolean;productionCertified?:boolean}={}):IntegrationSupportLevel{if(evidence.productionCertified)return"Production certified";if(evidence.hardwareValidated)return"Hardware validated";if(evidence.sandboxE2E)return"Sandbox E2E validated";if(evidence.runtimeExecuted||definition.maturity==="adapter")return"Runtime executable";if(evidence.credentialsValidated)return"Credentials validated";if(evidence.guided||definition.maturity==="setup")return"Guided setup";if(evidence.detected)return"Detected";return"Cataloged";}
